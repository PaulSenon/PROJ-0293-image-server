{
  "name": "localapigateway",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "npm run clean:dist && npm run clean:node_modules",
    "clean:dist": "rm -rf dist",
    "clean:node_modules": "rm -rf node_modules",
    "start": "node -r @swc-node/register src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "a small local mock api gateway",
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@swc-node/register": "^1.10.0",
    "@swc/core": "^1.6.5",
    "@types/node": "^20.14.9",
    "typescript": "^5.5.2"
  }
}
